const config = {
    port: 3000, // 포트 번호
    ip: '127.0.0.1', // IP 주소
    name: 'Bookmark Website Dev' // 이름
};

module.exports = config; // 모듈로 내보냅니다.


// NODE_ENV 설정 방법
// Windows
// SET NODE_ENV=production node index.js

// Mac/Linux:
// NODE_ENV=production node index.js
